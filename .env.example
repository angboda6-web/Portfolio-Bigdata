# Future database configuration
# Copy this file to .env when you switch from SQLite to PostgreSQL or another DB.

DATABASE_URL=sqlite:///warehouse/sales.db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=portfolio_db
DB_USER=postgres
DB_PASSWORD=change_me

